kind = "design-system"
previewPath = "/nextera-design-system/"
title = "Nextera Design System"
description = "A reusable visual language extracted from the Nextera Education website: ink-and-lime color roles, editorial display type, mono metadata, tactile borders, and purposeful motion for future Nextera experiences."
version = "1.0.0"
id = "artifacts/nextera-design-system"
router = "path"

[[services]]
name = "web"
paths = [ "/nextera-design-system/" ]
localPort = 23236

[services.development]
run = "pnpm --filter @workspace/nextera-design-system run dev"

[services.env]
PORT = "23236"
BASE_PATH = "/nextera-design-system/"
